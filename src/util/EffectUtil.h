// #include <Arduino.h>


// class EffectUtil
// {
//     public:
//         static int* getCenterTextCoords(String text, int displayWidth, int displayHeight, int fontWidth, int fontHeight)
//         {
//             int x = (displayWidth / 2) - (text.length() / 2 * displayWidth);
//             int y = (displayHeight / 2) - (fontHeight / 2 * fontHeight);

//             int result[2] = { x, y };
//             return result;
//         }

        
// };